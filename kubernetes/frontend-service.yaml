apiVersion: v1
kind: Service
metadata:
  name: frontend-lb
  namespace: yolo-app
spec:
  type: LoadBalancer
  selector:
    app: yolo-app
  ports:
  - port: 8080       # external port
    targetPort: 8000
